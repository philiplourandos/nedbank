---
management:
    endpoints:
        web:
            exposure:
                include: health, metrics, prometheus
    metrics:
        tags:
            application: ${spring.application.name}
spring:
    application:
        name: nedbank-sarb-service
    cache:
        cache-names: sarb-rates
za:
    co:
        nedbank:
            service:
                sarb:
                    url: https://custom.resbank.co.za
                    cron: 0 0 23 * * ?