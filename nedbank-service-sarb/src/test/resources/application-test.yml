## YAML Template.
---
spring:
    cache:
        cache-names: rates
    cloud:
        config:
            enabled: false
za:
    co:
        nedbank:
            service:
                sarb:
                    cron: 10 * * * * ?